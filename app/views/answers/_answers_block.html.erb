<div class="answers-block">
	<% @answers.each do |answer|%>
		<div class="answer">
			<p><%= answer.body %> – 
				<strong>
					<%= "#{answer.user.name} #{answer.user.last_name}" %>
				</strong>
				<%= time_ago_in_words(@question.created_at) + " назад" %>
			</p>
		<a href="#">Предложить правку</a>
		</div>
	<%end%>
 </div>